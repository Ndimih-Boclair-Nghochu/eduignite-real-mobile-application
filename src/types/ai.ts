/**
 * @/types/ai — Re-exports AI-related types from the canonical types module.
 * Keeps the import path @/types/ai working for components that use it.
 */
export type { AIRequest, AIInsight } from '@/lib/api/types';
